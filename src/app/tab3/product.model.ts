export interface Product {
	id: string;
	title: string;
	imageURL: string;
	comments: string[];
	price: number;
	previousPrice: number;
}

/* interface rubros{
	id: number
	rubro: string
} */ 